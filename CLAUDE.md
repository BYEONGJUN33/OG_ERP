# CLAUDE.md — 오픈가든 사내 포털

이 파일이 이 저장소의 규칙이다. HANDOFF.md는 배경, 이 파일은 규칙.
충돌하면 이 파일이 이긴다. 결정이 바뀌면 대화가 아니라 이 파일을 고친다.

## 1. 무엇을 만드는가

흩어진 사내 도구를 한 주소로 모으는 사내 포털. 지금은 ERP가 아니다.
1단계 기능은 셋뿐: 프로그램 카드 목록 / 캘린더 보기 / 할 일 읽기.

사용자는 2명(이시형, 배병준). 둘 다 전체 접근. 권한 구분 없음.

화면 구조는 `docs/ui.md`에 있다. 화면을 만들기 전에 그 파일을 읽어라.

### 장기 방향 — 이건 알고 설계해라

이 포털은 최종적으로 오픈가든의 통합 ERP가 된다. 1단계가 작은 건
범위가 작아서지 목표가 작아서가 아니다. 지금 내리는 구조 결정은
아래가 나중에 얹힐 수 있어야 한다.

- 할 일 ↔ 캘린더 통합 보기
- 연차·휴가·출장 등 신청서와 승인
- 근태, 그 밖의 인사 기능
- 기존 도구들의 포털 내부 이식과 데이터 통합

**목표: Airtable UI를 켤 일이 없게 만든다.** 사람이 쓰는 화면은 포털이고
Airtable은 뒤에서 데이터를 담는 곳이다. 새 화면을 만들 때마다 이걸 묻는다 —
"이 일을 하려고 여전히 Airtable을 열어야 하나?"

판단 기준: 지금 만들지 않는다. 다만 나중에 못 넣게 만들지도 않는다.

## 2. 불변 원칙 (어길 거면 먼저 물어라)

1. Airtable·Google API 키는 서버에서만 쓴다. 클라이언트 컴포넌트,
   `public/`, `NEXT_PUBLIC_*`에 키가 닿으면 안 된다. 예외 없다.
2. Airtable이 업무 데이터의 유일한 원천. 포털은 데이터를 자체 보관하지 않는다.
3. 외부 서비스가 주는 건 직접 만들지 않는다(인증, 캘린더 엔진, 파일 저장).
4. 화면은 수직 슬라이스로 하나씩 완성한다. 로그인→조회→표시까지 끝내고 다음.
5. 모바일 우선. 지자체 방문 현장에서 폰으로 본다.
6. 흰 화면 금지. 모든 데이터 화면은 3상태(로딩/비었음/실패)를 갖는다.
7. 화려함보다 평범하고 직관적으로.
8. 한 단계가 끝나면 멈추고 확인받는다. 여러 단계를 몰아서 하지 않는다.
9. 날짜 있는 데이터는 성격에 따라 원천이 갈린다.
   - 반복·장소·첨부·알림이 필요한 "일정" → 구글 캘린더가 원천.
     반복 전개, 공휴일, 첨부는 구글에 맡긴다. 직접 구현 금지.
   - 상태·승인·담당이 붙는 "업무 데이터" → Airtable이 원천.
   양쪽을 `PortalEvent`로 정규화해 한 화면에 겹친다. 동기화하지 않는다.
   Airtable 데이터를 구글 캘린더로 밀어넣는 코드를 쓰지 마라.

## 3. 손대지 않는 것 (발견해도 고치지 마라)

아래는 이미 알고 있는 문제다. "고치겠다"며 손대는 것을 금지한다.
기존 도구가 실사용 중이다. 2단계 이후 별도 작업.

- 품목이 5개 테이블에 흩어짐 (SKU 미통일, 공통 키 없음)
- 재고가 4군데 (`재고` 테이블 + 라인업 테이블들의 재고 필드)
- 제안서 생성기의 테이블=주제 구조
- 지자체 영업일지가 `거래처`와 링크 없이 텍스트로만 존재
- `작업자` 테이블 PIN 평문 저장, `api/stock.js`가 읽음 (사실상 미사용)
- `속 분류` 테이블 / 웹 카탈로그 속 구분 개편

기존 테이블 14개는 1단계에서 읽지도 쓰지도 않는다.
마주치면 고치지 말고 보고만 한다.

## 4. 스택

- Next.js (App Router) / TypeScript / Tailwind
- 인증: Auth.js v5 (`next-auth@5`), Google provider, `hd` 도메인 제한
- 데이터: Airtable REST (서버에서만)
- 일정: **자체 캘린더 뷰**(2026-09-14 교체). 서버가 구글 API로 읽어 직접 그린다.
  임베드는 버렸다 — 비공개 캘린더를 iframe에 띄우면 브라우저의 서드파티 쿠키
  차단에 걸려 "일정을 로드할 수 없습니다"가 뜬다. 서버에서 읽으면 쿠키가
  개입하지 않는다.
  캘린더는 한 화면에 겹쳐 본다. 다만 출처는 여럿이다 —
  공용 + 대한민국 공휴일 + 보는 사람 본인 것.
  "캘린더 1개"는 화면이 하나라는 뜻이지 출처가 하나라는 뜻이 아니다.
  **개인 캘린더를 `GOOGLE_CALENDAR_ID`에 넣지 마라.** 남의 개인 일정이
  모두에게 보이고, 공유 안 된 사람 화면에서는 빈 칸으로 나온다.
  본인 캘린더는 로그인 이메일로 코드가 붙인다 — 사람이 늘어도 설정을 안 고친다.
  달력 격자는 라이브러리 없이 직접 그린다(`month.ts` + `month-calendar.tsx`).
  어려운 부분(반복 전개)은 구글이 끝내서 보내주므로 남는 건 격자에 점 찍기뿐이다.
  구글 이벤트는 `singleEvents=true`로 읽는다 — 반복을 구글이 펼쳐 준다.
  RRULE 파싱 코드를 쓰지 마라. 공휴일은 구글 공휴일 캘린더 구독으로 해결.
  구글은 캘린더 엔진(반복·초대·알림·모바일 앱)을 계속 담당하고
  우리는 뷰만 만든다. 엔진을 만들지 마라.
- 배포: Vercel, `app.open-garden.co.kr`

## 5. 환경

- Node 22 (`.nvmrc`에 `22`). 확인된 실행 버전 v22.22.2 / npm 10.9.7
- Next.js 16 / React 19 / Tailwind v4 / ESLint 9 (2026-09-14 초기화 기준)
- 소스는 `src/` 아래. import alias는 `@/*` (= `src/*`)
- 최초 1회만 `npm install` (lock 파일 생성). 이후 항상 `npm ci`
- `npm run dev` / `npm run build` / `npm run lint`
- 세션 시작 시: `node -v` → `git remote -v` → `npm ci`
- 기본 브랜치는 `main`. 여기에 바로 쌓는다.

### 환경변수 (이름만 기록. 값은 절대 커밋 금지)

```
AIRTABLE_TOKEN
AIRTABLE_BASE_ID=appzLstFHtHcQlWSu
AUTH_SECRET
AUTH_GOOGLE_ID
AUTH_GOOGLE_SECRET
AUTH_URL              # 로컬만. Vercel에서는 생략
ALLOWED_HD=open-garden.co.kr
GOOGLE_CALENDAR_ID    # 쉼표로 여러 개. 맨 앞이 기본 캘린더
```

`.env.example`에는 위 이름만 둔다. `.env*`는 `.gitignore`에 넣는다.

## 6. Airtable

Base `appzLstFHtHcQlWSu` (OpenGarden). 1단계에서 쓰는 테이블은 2개뿐.
전체 지도는 `docs/airtable.md`.

### 프로그램 `tblPMe2YBiKlgHBEN`

`이름 / 설명 / 경로 / 아이콘 / 분류 / 노출순서 / 사용여부 / 새창열기 / 비고`

- `사용여부`(체크박스) = true인 행만, `노출순서`(숫자) 오름차순
- `경로`: 내부는 `/tools/quote.html`, 외부는 `https://` 전체 주소
- `아이콘`: lucide-react 이름 (`file-text`, `package`, `map-pin`)
- `분류`: 영업 / 문서 / 재고 / 지도 / 기타
- `새창열기`는 체크박스
- 새 도구 추가 = 이 테이블에 한 줄. 배포 불필요. 이게 이 설계의 핵심이다.

### 할일 `tblczfuo7BoHcmQou`

`내용 / 담당자 / 시작일 / 마감일 / 상태 / 완료일시 / 분류 / 거래처(링크) / 메모 / 작성자 / 생성일시 / 수정일시`

- `담당자`는 단일선택(`이시형` | `배병준`). 이메일 필드가 아니다.
- `상태`: 예정 / 진행중 / 완료 / 보류
- `분류`: 영업 / 수입 / 시공 / 자재 / 내부 / 기타
- `완료일시`는 dateTime, Asia/Seoul
- `작성자`는 포털이 자동으로 채운다(로그인한 사람). 사람이 고르지 않는다.
- `생성일시`·`수정일시`는 Airtable 자동 필드. 코드가 쓰지 않는다.
- 우선순위·시간추적·하위작업은 **의도적으로 만들지 않았다.**
  2인 조직에서 마감일이 곧 우선순위다. 필요해지면 그때 넣는다.
- 할 일과 한 일을 테이블로 나누지 않는다. 상태 + 완료일시로 구분.
- 쓰기 완료(2026-09-14): 포털에서 추가·상태변경·완료 처리.
  완료로 바꾸면 `완료일시`를 자동으로 찍고, 완료를 풀면 지운다.
- **쓰기는 2단계 1순위였다** — 포털에서 할 일을 추가·완료하지
  못하면 결국 Airtable을 열게 되고, 그러면 포털이 반쪽이 된다.

### 규칙

- Airtable 호출은 `src/lib/airtable/` 안에서만. 화면은 이 함수만 부른다.
- 클라이언트 컴포넌트가 쓰는 값·타입은 `src/lib/todo-types.ts`처럼
  `server-only`가 없는 파일에 둔다. 서버 모듈에서 가져오면 빌드가 막힌다
  (막히는 게 정상이다 — 원칙 1을 컴파일러가 지키는 것이다).
- 쓰기 뒤에는 `updateTag`로 해당 테이블 캐시를 즉시 버린다.
  방금 한 일이 화면에 안 보이면 사용자는 실패한 줄 알고 또 누른다.
- 서버 액션은 누구나 부를 수 있는 입구다. 매번 세션을 확인한다.
- 한글 필드명은 이 경계에서 한 번만 영문 타입으로 바꾼다.
  화면 코드에 한글 키 문자열이 나오면 안 된다(오타를 TS가 못 잡는다).
- 새로고침마다 전체 읽기 금지. 호출 한도가 있다(5 req/s).
  캐시: `프로그램` 600초 / `할일` 60초.
  캐시는 `unstable_cache`로 건다. `fetch`의 `next.revalidate`를 쓰면
  403 같은 실패 응답까지 캐시되어, 권한을 고쳐도 캐시 기간 내내
  같은 에러가 계속 보인다. **성공한 결과만 캐시한다.**
- 필요한 필드만 요청한다(`fields[]`). 전체 레코드를 통째로 끌지 않는다.
- **칸을 비울 때는 `null`을 보낸다.** 빈 문자열은 날짜·단일선택에서
  422로 거절당한다 — Airtable은 그걸 "비움"이 아니라 "잘못된 값"으로 읽는다.
  쓰기 타입은 `FieldValues<Row>`를 쓴다(`null` 허용).
- 날짜 비교를 Airtable 수식에 맡기지 마라. 수식의 `TODAY()`는 UTC 기준이라
  한국 시간 자정 근처에서 하루가 어긋난다. 넉넉히 가져와 코드에서 거른다.
- 조회 필터는 코드에 둔다. Airtable 뷰에 숨기지 않는다.
  (뷰는 사람이 표를 직접 고칠 때 쓰는 도구다)
- 새 테이블을 만들기 전에: 기존 테이블에 필드 3개로 해결되면 만들지 않는다.
  "주제마다 테이블"은 3번 목록에 적힌 기존 실수의 반복이다.
- 신청서류(연차/휴가/출장/기타)는 `신청서` 1개 + `유형` 단일선택으로 간다.
- 생일·경조사·공휴일은 Airtable에 넣지 않는다. 구글 캘린더에 넣는다.
- 테이블 이름은 `[포털] / [품목] / [영업] / [기타]` 접두어로 묶어 정렬한다.
  단 기존 도구가 깨질 수 있으므로 1단계에서는 이름을 바꾸지 않는다.
- 새 테이블을 만들면 `docs/airtable.md`에 한 줄 추가한다.

## 7. 로그인과 사용자

- Google OAuth. `hd=open-garden.co.kr`로 제한하고,
  콜백에서 이메일 도메인을 서버에서 한 번 더 검증한다(`hd`는 위조 가능).
- 이메일 → 담당자 이름 매핑은 `src/config/users.ts` 한 곳에만 둔다.
  `할일.담당자`가 단일선택 텍스트라 매핑이 필요하다.
  매핑에 없는 이메일은 로그인 거부.
- OAuth 스코프는 `src/lib/auth/scopes.ts` 한 곳에만 정의한다.
  `calendar.readonly` 포함(2026-09-14 추가).
- 구글 액세스 토큰은 1시간이면 만료된다. `access_type=offline` + `prompt=consent`로
  refresh token을 받아 만료 1분 전에 갱신한다. 갱신 실패하면 토큰을 비우고
  다시 로그인하게 둔다 — 깨진 토큰으로 계속 시도하면 원인 모를 에러만 반복된다.
- 토큰은 세션(JWT)에만 있고 서버에서만 읽는다. 화면으로 내려보내지 않는다.

## 8. 기존 HTML 도구

- 배치 완료: `quote.html`(견적서·발주서·거래명세서 3종), `proposal.html`(제안서).
  파일명만 ASCII로 바꿨고 내용은 한 글자도 고치지 않았다.
- 지자체 영업 지도는 개발 중이라 아직 없다. 웹사이트는 별도 저장소.
- 1단계에서는 코드를 고치지 않고 `public/tools/`에 그대로 둔다.
- 접근 통제는 서버 라우트 경유를 먼저 시도하고, 복잡해지면 공개로 둔다.
  공개로 두는 것은 허용된 선택이다.
- 예외 없는 검사: 파일을 저장소에 넣기 전에 반드시 키가 들어 있는지 본다.

```
grep -rniE "key|token|secret|Bearer|pat[A-Za-z0-9]{10}" public/tools/
```

  하나라도 나오면 커밋하지 말고 멈추고 보고한다. 원칙 1이 우선이다.

## 9. 코드 방침

- 단순하게. 추상화는 두 번째 사용처가 생겼을 때 만든다.
- 데이터 화면은 공용 `<DataState>` 하나로 로딩/비었음/실패를 처리한다.
  실패는 재시도 버튼을 준다. 흰 화면·무한 스피너 금지.
- 서버 컴포넌트를 기본으로. 클라이언트 컴포넌트는 상호작용이 있을 때만.
- 테스트 프레임워크는 1단계에 넣지 않는다.
  통과 기준은 `npm run lint`와 `npm run build`가 깨끗한 것.
- UI 문구는 한국어. 코드·변수·커밋 메시지는 영문.

### 캘린더 합성 구조

```ts
type PortalEvent = {
  id: string
  title: string
  start: string          // ISO
  end?: string
  allDay: boolean
  source: 'google' | 'airtable'
  type: '할일' | '연차' | '휴가' | '출장' | '구글일정'
  owner?: string
  location?: string
  description?: string
  href?: string          // 원본으로 가는 링크
}
```

- `src/lib/calendar/types.ts`에 `PortalEvent`를 둔다.
- `src/lib/calendar/sources/*.ts`에 출처별 `getEvents(from, to)` 하나씩.
- 캘린더 화면은 출처를 모른다. 합쳐진 배열만 받는다.
- 새 유형 추가 = 어댑터 파일 하나 + 등록 한 줄. 그 이상이면 설계가 틀린 것이다.
- 1단계에서는 타입만 정의하고 어댑터는 만들지 않는다.

## 10. 1단계 체크리스트

- [x] Next.js + TS + Tailwind 초기화, `.gitignore`, `.env.example`
- [x] Auth.js v5 + Google + 도메인 제한 (2026-09-14 실제 로그인 확인)
- [x] Airtable 읽기 함수 (`프로그램`, `할일`) — 서버 전용 (배포 환경 동작 확인)
- [x] 대시보드: 프로그램 카드 / 내 할 일 / 캘린더 임베드 (레이아웃은 잠정안)
- [x] 기존 HTML 도구를 `public/tools/`에 배치 (키 검사 후)
- [x] Vercel 프로젝트 배포 · 대시보드 동작 확인
- [ ] `app.open-garden.co.kr` 도메인 연결

하지 않는 것: 견적서 생성기 React 이식, 품목 통합, 기존 14개 테이블 접근.
(할 일 쓰기와 자체 캘린더 뷰는 1단계 범위 밖이었으나 앞당겨 끝냈다)

## 11. 데이터 저장 방침

Airtable로 간다. 2인·소량 데이터에 별도 DB는 과하다.
아래 신호가 하나라도 오면 그때 Postgres(Supabase/Neon) 이전을 검토한다.

- 한 테이블 5만 행 초과
- 캐시로도 API 한도(5 req/s)를 못 막을 때
- 동시 수정 충돌이 실제로 발생할 때
- 첨부 용량이 커질 때(→ Drive로 분리)

대비는 하나뿐: 모든 Airtable 접근이 `src/lib/airtable/`를 통과할 것.
화면 코드에 Airtable이 새어 나가면 이전 비용이 폭증한다.

## 12. 변경 기록

HANDOFF.md보다 이 파일이 최신이다.

2026-09-14
- 환경변수 `NEXTAUTH_SECRET`/`NEXTAUTH_URL` → Auth.js v5의 `AUTH_*`로 교체
- 캘린더 "용도별 분리 저장" → 캘린더 1개로 통합. 나누지 않는다.
- Spec Kit 도입 취소. 문서는 이 파일과 `docs/`에 손으로 둔다.
- `할일.담당자`가 협업자 필드가 아니라 단일선택임을 스키마에서 확인
- 캘린더 연동을 단방향 동기화에서 읽기 시점 합성으로 변경.
  동기화는 유형이 늘수록 어긋난다. `캘린더이벤트ID` 필드 계획 철회.
- `기념일` 테이블 계획 철회. 생일·경조사·공휴일은 구글 캘린더가 담당.
- 방향: 지금은 모으는 것이 목적. 장기적으로 통합 ERP로 확장한다.
- `src/` 레이아웃 채택. 문서의 `lib/`·`config/` 경로를 `src/` 기준으로 수정.
- 웹폰트(Geist)를 쓰지 않는다. 한글 우선 시스템 폰트 스택으로 대체.
- 조회 실패를 예외 대신 `Result<T>`로 돌린다(`src/lib/result.ts`).
  한 구획이 실패해도 나머지 구획은 그대로 보이게 하려는 것이다.
- Airtable 실패 응답이 캐시되던 문제 수정. 성공만 캐시한다.
- 할 일 쓰기 추가(추가·상태변경·완료). Airtable을 열지 않고 일한다.
- `할일`에 `시작일`·`작성자`·`생성일시`·`수정일시` 추가.
  Jira 이슈 필드를 참고하되 우선순위·시간추적·하위작업은 뺐다.
- 댓글 추가(`할일.댓글` 한 칸). 테이블을 따로 만들지 않는다 —
  한 건에 한두 개라 자식 테이블이 값어치보다 비싸다.
  읽고-고쳐-쓰기이므로 쓰기 직전에 캐시 없이 다시 읽는다.
- 환경변수는 읽을 때 `trim()`한다. 붙여넣을 때 딸려온 줄바꿈 하나로
  요청이 통째로 거절당한다(구글 400, Airtable 422).
- 임베드를 자체 뷰로 교체. 서드파티 쿠키 차단으로 비공개 캘린더가 iframe에서
  로드되지 않는 문제를 서버 읽기로 해결. `/calendar`에 월간 달력,
  홈에는 오늘 일정만.
- 캘린더 출처를 여럿 받는다(쉼표 구분). 맨 앞이 기본.
  본인 캘린더는 로그인 이메일로 자동으로 얹는다.
- 캘린더 [일정 추가]는 구글 등록 화면으로 넘긴다. 폼을 만들지 않는다.
  할 일을 캘린더로 보내는 버튼은 만들지 않는다(원칙 9).
- 칸 비우기가 422로 실패하던 버그 수정. 빈 문자열 → `null`.
- 할 일 상세 화면 추가(`/todos/[id]`). Jira식 좌: 내용 / 우: 속성 배치.
  상태만 즉시 저장하고 나머지는 [저장]으로 묶는다 —
  상태는 완료일시와 짝이라 따로 관리해야 한다.
- 홈을 '오늘' 화면으로 재구성: 내 할 일 / 팀 할 일 / 오늘 끝낸 일 /
  프로그램 / 캘린더. 내 것과 남의 것은 구획으로 나누고 섞지 않는다.
- 1단계 데이터 흐름 전 구간 검증 완료(로그인 → Airtable 읽기 → 화면).
  남은 것은 도메인 연결뿐.
- 대시보드 레이아웃은 잠정안이다. 1단계 직후 클로드 디자인으로
  시각 언어를 정하고 화면 구조도 다시 잡는다. 한 화면에 3구획을
  쌓은 지금 형태는 확정이 아니다.
- 배포 설정 3건을 `docs/deploy.md`에 기록: OAuth 클라이언트는 웹 애플리케이션
  유형이어야 하고, Vercel에 `AUTH_URL`을 넣으면 안 되며, 캘린더는 개인이 아닌
  공용 캘린더를 쓴다.
- 기존 HTML 도구 2개 배치. 키 검사 통과(하드코딩된 토큰 없음).
  두 도구는 사용자가 직접 입력한 Airtable PAT를 브라우저 localStorage에
  보관하고 프론트에서 Airtable을 직접 부른다. 원칙 1과 어긋나지만
  1단계는 무수정 배치가 결정이므로 고치지 않는다. 2단계 과제.
